/**
 * Export barrel for src/ui/icons/. Every hand-drawn stroke icon that isn't specific to one
 * component (those stay local, like Dock.tsx's SelectIcon/DrawLineIcon) joins this list so it has
 * one import path from the rest of src/ui/.
 */

export { Bus } from './Bus';
export type { BusIconProps } from './Bus';
